# 配置文件使用说明

## 概述

当需要配置多个参数时，使用命令行参数会不方便。此时可以使用JSON配置文件来管理所有配置。

## 使用方法

### 1. 基本使用

```bash
./access_log_analyzer -config config.json /path/to/logs
```

### 2. 配置文件与命令行参数组合使用

命令行参数优先级**高于**配置文件：

```bash
./access_log_analyzer -config config.json -top 20 /path/to/logs
```

这样会使用 `config.json` 中的所有配置，但 `top` 参数会使用命令行指定的 20。

## 配置文件格式

配置文件为JSON格式，所有字段都是可选的：

```json
{
  "fields": "sip,dip,domain",
  "top": 20,
  "sort": "down",
  "csv_top": 5000,
  "workers": 8,
  "batch_size": 100,
  "output": "result.csv",
  "log_path": "/path/to/logs",
  "start_time": "20260330000000",
  "end_time": "20260331235959",
  "sip_filters": [
    "192.168.1.100",
    "10.0.0.*"
  ],
  "dip_filters": [
    "123.180.157.193",
    "123.180.157.192"
  ],
  "domain_filters": [
    "*.example.com",
    "test.com"
  ],
  "sport_filters": [
    "80",
    "443"
  ],
  "dport_filters": [
    "8080"
  ],
  "url_filters": [
    "^/api/v[0-9]+/",
    ".*\\.json$"
  ],
  "sip_reverse": false,
  "dip_reverse": false,
  "domain_reverse": false,
  "sport_reverse": false,
  "dport_reverse": false,
  "url_reverse": false,
  "sip_filter_mode": 0,
  "dip_filter_mode": 0,
  "domain_filter_mode": 0,
  "sport_filter_mode": 0,
  "dport_filter_mode": 0,
  "url_filter_mode": 0
}
```

### 字段说明

#### 统计参数

- `fields`: 统计字段，用逗号分隔（默认: "dip,domain"）
- `top`: 显示Top N条记录（默认: 10）
- `sort`: 排序方式：up(上行流量), down(下行流量), total(总流量)（默认: "up"）
- `csv_top`: CSV文件导出最大行数（默认: 1000, 0表示全部）
- `workers`: 并发协程数（默认: 4）
- `batch_size`: 每批处理的文件数量后生成临时CSV（默认: 0表示不生成）
- `output`: 输出CSV文件名（默认: 自动生成）
- `log_path`: 日志文件路径

#### 时间过滤参数

- `start_time`: 开始时间，格式 YYYYMMDDHHmmss（精确到秒，可选）
- `end_time`: 结束时间，格式 YYYYMMDDHHmmss（精确到秒，可选）
- 示例：`"20260330000000"` 表示2026年3月30日00:00:00
- 示例：`"20260331235959"` 表示2026年3月31日23:59:59

#### 过滤参数

- `sip_filters`: 源IP过滤列表（可选）
- `dip_filters`: 目的IP过滤列表（可选）
- `domain_filters`: 域名过滤列表（可选）
- `sport_filters`: 源端口过滤列表（可选）
- `dport_filters`: 目的端口过滤列表（可选）
- `url_filters`: URL过滤列表（可选，支持正则表达式，匹配base64解码后的URL）

#### 反向过滤参数

- `sip_reverse`: 源IP反向过滤，true表示排除匹配的项（默认: false）
- `dip_reverse`: 目的IP反向过滤，true表示排除匹配的项（默认: false）
- `domain_reverse`: 域名反向过滤，true表示排除匹配的项（默认: false）
- `sport_reverse`: 源端口反向过滤，true表示排除匹配的项（默认: false）
- `dport_reverse`: 目的端口反向过滤，true表示排除匹配的项（默认: false）
- `url_reverse`: URL反向过滤，true表示排除匹配的项（默认: false）

#### 空值过滤模式

- `sip_filter_mode`: 源IP空值过滤模式（默认: 0）
- `dip_filter_mode`: 目的IP空值过滤模式（默认: 0）
- `domain_filter_mode`: 域名空值过滤模式（默认: 0）
- `sport_filter_mode`: 源端口空值过滤模式（默认: 0）
- `dport_filter_mode`: 目的端口空值过滤模式（默认: 0）
- `url_filter_mode`: URL空值过滤模式（默认: 0）

空值过滤模式说明：
- `0`: 统计所有（默认）
- `1`: 只统计空值（字段为空或"-"）
- `2`: 只统计非空值

### 匹配规则

支持精确匹配和 `*` 通配符：

- `192.168.1.100` - 精确匹配该IP
- `10.0.0.*` - 匹配 10.0.0.0 ~ 10.0.0.255
- `*.example.com` - 匹配所有 example.com 的子域名

端口过滤支持精确匹配和 `*` 通配符：

- `80` - 精确匹配端口80
- `80*` - 匹配以80开头的端口（如80, 8080, 8081等）

URL过滤支持正则表达式（匹配base64解码后的URL）：

- `^/api/` - 匹配以 /api/ 开头的URL
- `.*\.json$` - 匹配以 .json 结尾的URL
- `^/api/v[0-9]+/` - 匹配 /api/v1/, /api/v2/ 等

## 示例

### 示例1：完整配置

```json
{
  "fields": "sip,dip,domain",
  "top": 20,
  "sort": "down",
  "csv_top": 5000,
  "workers": 8,
  "batch_size": 100,
  "output": "result.csv",
  "sip_filters": ["192.168.1.*"],
  "dip_filters": ["123.180.157.193"],
  "domain_filters": ["*.example.com"]
}
```

使用方式：
```bash
./access_log_analyzer -config config.json
```

### 示例2：仅配置过滤条件

```json
{
  "sip_filters": ["192.168.1.100", "192.168.1.101"],
  "dip_filters": ["123.180.157.193"]
}
```

使用方式：
```bash
./access_log_analyzer -config config.json /path/to/logs
```

### 示例3：配置文件 + 命令行覆盖

配置文件 `config.json`:
```json
{
  "fields": "dip,domain",
  "top": 10,
  "sort": "up"
}
```

命令行覆盖：
```bash
./access_log_analyzer -config config.json -fields "sip,dip,domain" -top 20 -sort down /path/to/logs
```

最终使用：
- fields: "sip,dip,domain" (命令行覆盖)
- top: 20 (命令行覆盖)
- sort: "down" (命令行覆盖)

### 示例5：端口过滤

```json
{
  "fields": "dip,domain",
  "top": 20,
  "sport_filters": ["80", "443"],
  "dport_filters": ["8080", "8443"]
}
```

使用方式：
```bash
./access_log_analyzer -config config.json /path/to/logs
```

或使用命令行：
```bash
./access_log_analyzer -sport "80,443" -dport "8080,8443" /path/to/logs
```

### 示例6：URL过滤（正则表达式）

```json
{
  "fields": "dip,domain",
  "top": 20,
  "url_filters": ["^/api/v[0-9]+/", ".*\\.json$"]
}
```

使用方式：
```bash
./access_log_analyzer -config config.json /path/to/logs
```

或使用命令行：
```bash
./access_log_analyzer -url "^/api/v[0-9]+/,.*\\.json$" /path/to/logs
```

### 示例7：反向过滤

```json
{
  "fields": "dip,domain",
  "top": 20,
  "dip_filters": ["10.0.0.1"],
  "dip_reverse": true,
  "domain_filters": ["test.com"],
  "domain_reverse": true
}
```

使用方式：
```bash
./access_log_analyzer -config config.json /path/to/logs
```

或使用命令行：
```bash
./access_log_analyzer -dip "10.0.0.1" -dip_reverse -domain "test.com" -domain_reverse /path/to/logs
```

效果：排除目的IP为10.0.0.1且域名为test.com的日志。

### 示例8：空值过滤模式

```json
{
  "fields": "dip,domain",
  "top": 20,
  "domain_filter_mode": 1
}
```

使用方式：
```bash
./access_log_analyzer -config config.json /path/to/logs
```

或使用命令行：
```bash
./access_log_analyzer -domain_filter_mode 1 /path/to/logs
```

效果：只统计域名为空（或"-"）的日志。

空值过滤模式说明：
- `0`: 统计所有（默认）
- `1`: 只统计空值（字段为空或"-"）
- `2`: 只统计非空值

### 示例9：组合过滤

```json
{
  "fields": "dip,domain",
  "top": 20,
  "sport_filters": ["80", "443"],
  "dport_filters": ["80", "443"],
  "url_filters": ["^/api/"],
  "domain_filter_mode": 1
}
```

效果：统计源端口或目的端口为80/443，URL以/api/开头，且域名为空的日志。

### 示例10：空值分摊功能

在merge模式下，可以将空值记录的流量按比例分摊到其他非空记录上：

```bash
./access_log_analyzer -merge /path/to/csv_dir -fields "dip,domain" -top 10 -redistribute_empty
```

效果：
- 如果Top10中存在domain为空的记录（如"-"或空字符串）
- 程序会将这些空值记录的上行流量、下行流量、总流量和流数按比例分摊到其他非空记录上
- 分摊比例基于各非空记录的流量占比
- 最终输出Top9（因为空值记录被移除并分摊）

如果Top10中没有空值记录，则正常输出Top10。

## 注意事项

1. 配置文件中的所有字段都是可选的，可以只指定需要的配置
2. **命令行参数优先级高于配置文件**（除了过滤参数会合并）
3. 过滤参数（sip_filters, dip_filters, domain_filters, sport_filters, dport_filters, url_filters）会与命令行参数**合并**（取并集）
4. JSON文件必须符合标准JSON格式
5. 可以使用示例文件 `filter_config.example.json` 作为模板
6. log_path 可以通过配置文件、命令行参数或位置参数指定
7. URL过滤支持正则表达式，日志中的URL字段是base64编码的，会自动解码后匹配
8. URL正则表达式在启动时预编译，如果正则表达式格式错误，程序会启动失败
9. **BadgerDB存储引擎**：当日志量大且key多时，使用 `-use_badger` 参数可降低内存占用，数据存储在临时目录，处理完成后自动清理

## BadgerDB存储引擎

### 使用场景
- 日志文件数量多（数百个以上）
- 统计key组合多（百万级以上）
- 服务器内存有限，需要控制内存峰值

### 使用方法
```bash
./access_log_analyzer -use_badger -fields "dip,domain" -top 10 /path/to/logs
```

### 工作原理
- 使用BadgerDB（LSM树KV数据库）存储统计数据
- 数据持久化到磁盘，内存仅缓存热点数据
- 批量写入优化，每1000条记录刷新一次
- 处理完成后自动清理临时数据库文件

### 性能特点
- 内存占用降低 70-90%
- 写入性能优于CSV方案 5-10倍
- 自动管理内存和磁盘平衡
- 支持并发安全写入


## 批量执行
ansible-playbook log_analyzer_with_download.yml 
ansible-playbook log_export_with_download.yml 


## 示例4：批量分析多个设备
```bash
ansible IS -m shell -a 'rm -rf /home/sino/access_log_analyzer_release/config.json' -b
ansible IS -m copy -a 'src=/home/sino/access_log_analyzer_release/config.json dest=/home/sino/access_log_analyzer_release/' -b

#删除旧的日志记录文件
#ansible IS -m shell -a 'rm -rf /home/sino/access_log_analyzer_release/analyzer_2026042800.log' -b

#批量在每个设备上执行日志分析工具， -log_path 指定日志路径
ansible IS -m shell -a 'rm -rf /home/sino/access_log_analyzer_release/access_log_analyzer_linux_amd64' -b
ansible IS -m copy -a 'src=/home/sino/access_log_analyzer_release/access_log_analyzer_linux_amd64 dest=/home/sino/access_log_analyzer_release/' -b
ansible IS -m shell -a 'chmod +x /home/sino/access_log_analyzer_release/access_log_analyzer_linux_amd64' -b
0x31+0x03a0+000+DPI00251+SINO+2178+2026050620
#执行统计命令
ansible IS -m shell -a 'cd  /home/sino/access_log_analyzer_release && nohup ./access_log_analyzer_linux_amd64 -log_path /home/sino/fangwenrzhi -fields "dip,domain" -start 20260506200000 -end 20260506210000 -output result_2026050600.csv > ./analyzer_2026050600.log 2>&1 &' -b


ansible IS -m shell -a 'cd  /home/sino/access_log_analyzer_release && nohup ./access_log_analyzer_linux_amd64_new  -log_path /home/sino/fangwenrzhi -export -start 20260506195000 -end 20260506211000 -export_start 20260506200000 -export_end 20260506210000 -dip "240e:b1:9801:40c:3::16" -sip "240e:b1:9801:40c:3::16" -export_filter_logic 1 > ./analyzer_2026050700.log 2>&1 &' -b

#查看每个设备执行情况
ansible IS -m shell -a 'tail -n 10 /home/sino/access_log_analyzer_release/analyzer_2026050600.log' -b

#统计每个设备生成的csv文件
ansible IS -m shell -a 'ls -l /home/sino/access_log_analyzer_release/*2026050600*.csv' -b
ansible IS -m shell -a 'ls -l /home/sino/access_log_analyzer_release/export*.csv' -b

ansible IS -m shell -a 'rm -rf /home/sino/access_log_analyzer_release/export*.csv' -b

ansible IS -m shell -a 'ls -l /home/sino/fangwenrzhi/0x31+0x03a0+000+DPI00251+SINO+2178+2026050620*.tar.gz' -b
#将每个设备的记录下载到接口机
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/result_2026050600_down.csv dest=/home/sino/2026050600/{{ inventory_hostname }}_result_down.csv flat=yes"
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/result_2026050600_up.csv dest=/home/sino/2026050600/{{ inventory_hostname }}_result_up.csv flat=yes"

#可以将20260415_19_24_new目录下的文件打包回本地进行汇聚 ，也可以直接在接口机上汇聚
./access_log_analyzer_linux_amd64 -merge /home/sino/2026050600/ -fields "dip,domain" -top 10 -duration 3600

-dip "240E:0940:0209:*,240E:0940:020B:*,27.128.213.*,106.118.26.*,106.118.27.*"


ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/export_sip_240E:00B1:9801:040C:0003:0000:0000:0016_dip_240E:00B1:9801:040C:0003:0000:0000:0016_20260506200000_20260506200200_1.csv dest=/home/sino/2026050701/{{ inventory_hostname }}/ flat=yes"
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/export_sip_240E:00B1:9801:040C:0003:0000:0000:0016_dip_240E:00B1:9801:040C:0003:0000:0000:0016_20260506200000_20260506200200_2.csv dest=/home/sino/2026050701/{{ inventory_hostname }}/ flat=yes"
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/export_sip_240E:00B1:9801:040C:0003:0000:0000:0016_dip_240E:00B1:9801:040C:0003:0000:0000:0016_20260506200000_20260506200200_3.csv dest=/home/sino/2026050701/{{ inventory_hostname }}/ flat=yes"
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/export_sip_240E:00B1:9801:040C:0003:0000:0000:0016_dip_240E:00B1:9801:040C:0003:0000:0000:0016_20260506200000_20260506200200.csv dest=/home/sino/2026050701/{{ inventory_hostname }}/ flat=yes"


ansible IS -m shell -a 'tail -n 10 /home/sino/access_log_analyzer_release/analyzer_2026050700.log' -b
ansible IS -m fetch -a "src=/home/sino/access_log_analyzer_release/result_2026050600_up.csv dest=/home/sino/2026050600/{{ inventory_hostname }}_result_up.csv flat=yes"


ansible IS -m shell -a 'cd  /home/sino/access_log_analyzer_release && nohup ./access_log_analyzer_linux_amd64_new  -log_path /home/sino/fangwenrzhi -export -start 20260506195500 -end 20260506200500 -export_start 20260506200000 -export_end 20260506200200 -dip "240E:00B1:9801:040C:0003:0000:0000:0016" -sip "240E:00B1:9801:040C:0003:0000:0000:0016" -export_filter_logic 1 > ./analyzer_2026050700.log 2>&1 &' -b